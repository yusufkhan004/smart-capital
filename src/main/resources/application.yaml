server:
  port: 8081
  servlet:
    context-path: /api
spring:
  application:
    name: SmartCapital
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smart-capital?useSSL=false&serverTimezone=UTC
    username: root
    password: "#Ayub1234"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true